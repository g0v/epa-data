// Generated by LiveScript 1.2.0
var api_url, request, cache;
api_url = 'http://jimmyhub.net:9192/?address=';
request = require('request');
cache = {};
module.exports.geoJSON = function(addr, cb){
  if (!cb) {
    return;
  }
  if (cache[addr]) {
    return cb(null, cache[addr]);
  }
  return request(api_url + addr, function(err, response, body){
    var data;
    if (err) {
      return cb(err);
    }
    data = JSON.parse(body);
    if (data.length < 2) {
      return cb(err);
    }
    cache[addr] = [data[1].cx, data[1].cy];
    return cb(null, cache[addr]);
  });
};